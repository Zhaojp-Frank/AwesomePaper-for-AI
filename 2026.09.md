## 论文综述 Edge0
The Other Half of the Memory Wall:Serving 35B MoEs from SSD with Trained Routing Prediction

https://arxiv.org/pdf/2609.18063v1 AutoARK 2026.9.16

1. 针对消费级硬件上MoE模型因内存受限无法完整加载的难题，研究团队提出了 Edge0 推理引擎，通过将模型权重部署在 SSD 上并利用 mmap 实现按需流式读取，成功将 35B 参数规模的 MoE 模型内存占用降至 3 GiB 以内。
2. 核心观察发现，传统的即时加载会导致 SSD 延迟阻塞计算，为此引入了**跨Token 的 prerouter 机制**，利用per-layer head在当前 Token 对下一层路由进行超前预测，确保SSD读取与模型计算实现重叠，从而有效解决了层间依赖导致的推理停顿。
3. int4基础权重结合未合并（unmerged）的 recovery LoRA 适配器，Qwen3.6-35B-A3B；保持fp16教师模型相近精度的同时，在 24 GB 内存的 Mac mini M4 Pro 上实现了约 20 tok/s 的推理速度，较传统方案在不同路由宽度下带来了 80% 至 84% 的性能提升。

<img width="977" height="505" alt="image" src="https://github.com/user-attachments/assets/6c56adc0-1e3b-41d9-9521-eb1bee73a291" />
<img width="833" height="381" alt="image" src="https://github.com/user-attachments/assets/d3655b25-35e2-4b41-b310-fd4ec4395fd4" />
<img width="951" height="477" alt="image" src="https://github.com/user-attachments/assets/fa5d438e-ef0e-4e9b-bc17-1426ffe86dbf" />

#### 场景与具体问题
随着大语言模型规模的不断扩大，**Memory Wall（内存墙）**成为在消费级硬件（如24GB显存的桌面设备）上部署大规模模型的主要障碍。对于Mixture-of-Experts (MoE) 模型，尽管稀疏性降低了计算量，但其巨大的参数规模（如35B模型即使在4-bit量化下也占用近20GB）仍需完全驻留内存，挤压了KV Cache及OS的运行空间。若将模型权重卸载至SSD，则会因为专家权重（Expert weights）的加载延迟导致严重的推理停顿。

#### 业界存在的不足
现有的离线卸载方案（如 `llama.cpp` 的 `--cpu-moe`、PowerInfer、MoE-Infinity）大多关注如何通过缓存策略减少内存占用，但这些方案在解码过程中，Layer $N+1$ 的专家选择严重依赖 Layer $N$ 的输出，导致加载操作无法在计算执行前完成，从而产生致命的串行磁盘访问延迟。此外，现有的量化与合并策略（如将LoRA合并至量化基座并重量化）会导致模型精度显著下降。

#### 关键观察与假设
1. **预路由预测（Prediction-as-Routing）**：作者提出一个关键洞察——如果能在当前时间步（Token $t$）预测出下一个时间步（Token $t+1$）的路由选择，即可实现“计算与I/O重叠”。
2. **LoRA与量化不兼容性**：直接合并LoRA并对Int4基座进行重量化会损失LoRA权重的大部分效应。因此，应将LoRA作为并行Delta分支保留，不进行合并。
3. **预测即路由**：将预路由的输出直接作为后续层的真实路由选择，避免了传统预门控（Pre-gating）机制中运行时丢弃Token或进行Fallback的复杂性。

#### 方法核心思路与主要步骤
Edge0 采用了一种流式MoE推理框架，核心步骤如下：
1. **Prerouter（预路由）头设计**：在每层添加一个小型的感知头，根据当前 Token 的隐状态和历史路由信息，预测下一层在下一 Token 的专家选择。
2. **SSD流式传输**：使用 `mmap` 将量化的专家权重映射至内存。通过“双缓冲”和“增量栈（Incremental Stack）”机制，仅更新发生变化的槽位，大幅减少Tensor组装成本。
3. **Unmerged Recovery LoRA**：将 LoRA 保持为非合并状态，在计算中通过 $y = W_{int4}(x) + \frac{\alpha}{r} BAx$ 的形式并行叠加。
4. **三阶段训练策略**：
   - 第一阶段：对预路由头进行蒸馏，学习模仿原始路由逻辑。
   - 第二阶段：在“学生路径”（基于预路由）下进行SFT，使得模型适应量化与近似路由环境。
   - 第三阶段：基于策略梯度进行在线蒸馏，进一步提升在近似路由下的文本生成表现。

#### 实现细节
Edge0 基于 **MLX** 框架实现。其系统架构定义了一个统一的 `core/nn/io/quant` 接口，将模型定义与底层的流式专家调度解耦。系统通过 `staged slots` 机制，在解码阶段维护一个固定的内存槽位表，避免了每一层重新构建堆叠张量的开销。

#### 实验对比与结果
*   **模型与硬件**：测试了 35B（Qwen3.6-35B-A3B）和 8B 模型。实验在 MacBook M2（16GB RAM，未配置额外内存限制）和 Mac mini M4 Pro（24GB RAM）上进行。
*   **对比基线**：与全量加载的 fp16 模型基准、以及标准的按需读取（On-demand streaming）方案进行对比。
*   **关键结果**：
    - 在 24GB 机器上，Edge0 以 3GB 的活跃内存占用实现了 ≈20 tok/s 的解码速度。
    - 预路由机制相比纯按需读取方案，在 K=4 配置下实现了 **+82%** 的推理吞吐提升。
    - 质量评估方面，通过 OpenCompass 测试，Edge0 35B 较 fp16 基准平均仅落后约 3.9 个点，证明了量化加预路由方案的可行性。

#### 潜在局限性
1. **请求并发限制**：Edge0 目前仅支持 FIFO 串行处理，尚未针对多请求批处理（Batching）进行优化。
2. **CPU侧算力开销**：由于解码过程中存在图构建开销（约44ms/步），对于极高性能需求场景仍有改进空间。
3. **长链推理损耗**：在 AIME 等复杂推理任务中，相比 fp16 原生模型仍存在一定的精度差距（如 35B  tier 在 AIME 上下降 6.1 个点），表明长程推理对近似路由的鲁棒性尚需加强。


## 论文综述 SQD

Rethinking Heterogeneous System Disaggregation for Subquadratic Attention

https://arxiv.org/pdf/2609.13134 NVIDIA 2026.9.11

1. 针对大模型在长上下文推断中受限于内存带宽和计算冗余的挑战，研究发现subquadratic的Attention（稀疏 线性等）和FFN在 decode 阶段具有较低的算术强度和固定的内存占用，适合在 SRAM-only 加速器上运行。
2. 提出 SQD（SubQuadratic Disaggregation），将 decode 阶段细粒度拆分为由 **GPU 处理的Attention** 和由**ASIC处理的子二次项 Attention 及 FFN**，并配合IndexShare预取机制隐藏通信开销，通过 colocation 技术实现 prefill 与 decode 的流水线优化。
3. SGLang，8×B200GPU集群，Rubin+LPX（建模分析），GLM-5.2、Nemotron 3 Ultra 和 Gemma4-31B 模型，相比 GPU-only 基线在 **tokens/J 上提升了 31% 至 56%**，并在固定功率约束下实现了 1.2 倍至 3.6 倍的吞吐量增长，有效降低了低延迟场景下的推断成本。



次二次方注意力和 FFN 放置在仅 SRAM 的 ASIC 池中，从而在IndexShare layer group的每个 token 上仅进行两次激活传输。
<img width="1423" height="547" alt="image" src="https://github.com/user-attachments/assets/c0cbb910-eb9e-472f-9cf1-271b79704cfc" />

![Uploading image.png…]()

#### 1. 场景与具体问题
随着 Frontier LLMs (大语言模型) 在长上下文（Long-context）和复杂推理任务中的应用，模型推理成本日益攀升。推理阶段分为 **Prefill**（计算密集型）和 **Decode**（内存带宽受限型）。当前主流的推理系统往往忽略了现代大模型架构中 **Subquadratic Attention**（次二次项注意力）机制带来的不同算术强度（Arithmetic Intensity）和内存占用（Memory Footprint）特征，导致在异构系统（DRAM-based GPU 与 SRAM-only ASIC）上的部署效率低下。

#### 2. 业界存在的不足
*   **粗粒度分片限制**：现有的 **Attention-FFN Disaggregation** 方案通常将整个 Attention 模块留在 GPU，FFN 放在 ASIC，这导致在推理的每一层都要进行繁重的激活值（Activation）传输，在高频交互场景下严重阻塞流水线。
*   **硬件利用率失配**：传统 GPU 在执行 Decode 时，由于内存带宽限制，其庞大的计算资源（Tensor Cores）大量空置，且功耗无法随负载有效降低。
*   **忽视 Attention 结构异质性**：未能充分利用不同 Attention 变体（如 Sparse, Linear, Sliding-window）在内存驻留和计算特征上的差异。

#### 3. 关键观察与假设
*   **算术强度分布**：除 FFN 外，无论是 Quadratic Attention 还是 Subquadratic Attention，在 GPU 上均表现为内存受限。但在 SRAM-only ASIC 上，其算术强度更接近硬件的 **Ridge Point**，利用率更高。
*   **内存占用差异**：Quadratic Attention 的 KV Cache 随上下文长度线性增长，而 Subquadratic Attention 的内存需求是静态的（与长度无关）。
*   **Locality 假设**：Sparse Attention 中的 Top-k 选区具有空间局部性，且在分层计算中存在跨层共享（IndexShare），这为利用 interconnect slack 掩盖通信延迟提供了理论基础。

#### 4. 方法核心思路（SQD）
**SQD (SubQuadratic Disaggregation)** 提出了一种细粒度的分片策略：
*   **Quadratic/Subquadratic 分离**：将 Prefill 和所有 Quadratic Attention 部署在 DRAM-based GPU（共享 KV Cache）；将 Subquadratic Attention 层与 FFN 部署在 SRAM-only ASIC。
*   **Latency-aware 通信优化**：设计了 **IndexShare 预取机制**，通过利用 Subquadratic 层之间的计算冗余和 slack 时间，掩盖跨设备通信延迟。
*   **公式定义**：
    *   算术强度 $AI = \frac{FLOPS}{Bytes}$，SQD 通过将 Decode 阶段中低 intensity 的部分迁移至高带宽 SRAM，使 $AI$ 逼近设备阈值 $R$。
    *   通信掩盖条件：对于 IndexShare 组中 offset 为 $j$ 的层，Slack 定义为 $slack_j = (j-1) \cdot t_{layer} - L_0$，要求 $h_j \ge 1 - \frac{slack_j \cdot W}{B \cdot k \cdot e}$（其中 $W$ 为 interconnect 带宽，$B$ 为 batch size，$k$ 为 Top-k 大小，$e$ 为 entry 大小）。

#### 5. 实现细节
*   **框架与模块**：基于 **SGLang** 框架进行二次开发。修改了运输层（Transport Layer），支持跨设备激活值传输；在算子层引入了针对不同模型（如 GLM-5.2, Nemotron 3 Ultra, Gemma 4）的切分 Hook。
*   **通信优化**：弃用了传统的双向 rendezvous 同步，改用 **One-sided put + Doorbell** 原语，配合计算图捕获（Graph Capture）和 step contract，最小化 Launch gap 和同步开销。

#### 6. 实验对比
*   **模型与基线**：测试了 GLM-5.2, Nemotron 3 Ultra, Gemma 4-31B。对比了 No-disagg (GPU), Spec-dec (EAGLE-3), Attn-FFN Disagg。
*   **硬件配置**：采用 8×B200 GPU 集群作为 proxy，通过 MPS (Multi-Process Service) 限制 SM 数量以模拟 SRAM-only ASIC 算术强度，并建立 **Rubin + LPX** 异构系统分析模型。
*   **负载**：覆盖 Chat (32K), RAG (256K), Agentic (1M) 三种上下文长度模式。
*   **关键结果**：
    *   在能量效率上，SQD 在 GLM-5.2 上 Token/J 提升 53%，Gemma 4-31B 提升 56%。
    *   在固定功耗预算下，SQD 的用户吞吐量（TPS）比 Attn-FFN 分片方案高出 1.2× 到 1.5×，在特定负载下甚至达到 3.6×。

#### 7. 潜在局限性
*   **灵活性受限**：SQD 高度依赖模型架构对层类型的定义，若模型结构频繁演进（如引入非对称 Attention 层），需要重新调整分片逻辑。
*   **Interconnect 依赖**：虽对带宽不敏感，但对 Fabric 的单向延迟极为苛刻，要求异构互联方案必须保持极高的稳定性。
*   **预测准确性**：虽然 Analytical Model 与 B200 实测误差在 15% 以内，但面对未来 SRAM 更大、Fabric 更复杂的芯片，跨设备调度策略的复杂性可能会带来额外的排队抖动。
  
## 论文综述 To Copy or Not to Copy

To Copy or Not to Copy: Controlling Speculative Decoding via Intrinsic Model Signals

https://arxiv.org/pdf/2609.20186 2026.9.17

1. 针对大模型在推理过程中因表面n-gram重合导致的“偶然重复”与“结构化复制”难以区分，进而引发Speculative Decoding（SD）性能下降的瓶颈，本文分析发现基于启发式策略的非神经方法会产生严重的 speculation tax。
2. 发现模型内部隐藏层表征中存在可提取的“复制意图”信号（copy-intent）即归纳头（Induction Heads），并据此可针对性微调训练 利用最后输出时增加轻量级线性分类探测来动态识别解码模式，在处理Coding、Math Reasoning等任务时能精准触发上下文复制或切换至EAGLE3神经草稿模型。
3. Llama-3和Qwen-3系列模型, H100 GPU, 在维持生成质量的同时，相较于EAGLE3等SOTA基线实现了最高15%吞吐量提升，有效将噪声启发式策略转化为鲁棒的、模型感知的决策机制。

<img width="1201" height="379" alt="image" src="https://github.com/user-attachments/assets/143433b6-ac7f-44f2-87c7-689afddcf813" />

<img width="741" height="391" alt="image" src="https://github.com/user-attachments/assets/f8b4db2d-3da8-459d-afdc-0ce98f1cbbd2" />

<img width="1127" height="331" alt="image" src="https://github.com/user-attachments/assets/28ec3646-fc05-400e-ac06-b3c7c2e3ab31" />

#### 1. 场景与问题定义
在当前大语言模型（LLM）推理中，自回归解码的顺序性限制了其性能扩展。**Speculative Decoding (SD)** 通过引入轻量级草稿模型来并行验证多个Token，显著降低了延迟。目前存在两种主流草稿策略：**神经草稿（Neural Drafting）**（如EAGLE3）具有泛化性，但在处理长重复文本时效率不足；**基于上下文的复制（Context-based Copying）**（如Prompt Lookup Decoding）在处理文档抄写或代码生成时速度极高，但极易触发“偶然重复（Accidental Repetition）”。

#### 2. 业界现状与痛点
现有复制类方法（如CopySpec、BanditSpec）多依赖于n-gram的表面重合度作为触发信号。然而，这种启发式逻辑存在严重缺陷：
*   **假阳性触发：** 在数学推理等任务中，即便出现n-gram匹配，LLM的真实逻辑往往是推理而非复制。错误触发会导致草稿被模型大量拒绝，不仅浪费算力，反而拖慢推理速度，产生显著的“预测税（Speculation Tax）”。
*   **黑盒优化的低效：** 基于强化学习或多臂老虎机（Bandit）的方法因缺乏对模型内部状态的理解，需要在生成过程中频繁“探索”以获取性能反馈，导致巨大的性能开销。

#### 3. 关键观察与假设
本研究的核心假设是：**“复制意图”并非外部的文本属性，而是模型内部的一种潜变量模式。** 作者观察到，LLM在进行复制时会激活特定的归纳头（Induction Heads），这种逻辑意图被线性地编码在Transformer的隐藏表示（Hidden Representations）中。因此，可以通过探测器（Probe）直接识别这种潜层意图，从而在复制策略与生成策略之间实现精准切换。

#### 4. 核心方法论：SwitchSD
SwitchSD构建了一个自适应的“白盒”切换框架，通过轻量级线性探测器识别模型当前的解码状态。

*   **线性探测器训练：**
    针对第 $\ell$ 层的隐藏表示 $H^{(\ell)}_j$，训练一个分类器以预测当前是否处于复制模式：
    $$p_\ell(j) = \sigma(w^\top_\ell H^{(\ell)}_j)$$
    该探测器通过二元交叉熵损失函数进行训练。实验发现，在注意力层输出处进行的探测效果最佳。

*   **切换决策规则：**
    仅当同时满足以下两个条件时，系统才启动复制模式：
    1. 探测器输出高于设定阈值 $\tau$（即 $PR(H^{(\ell)}_i) \geq \tau$）。
    2. 上下文中确实存在长度至少为 $\kappa$（例如 $\kappa=5$）的n-gram匹配。
    如果探测器显示无复制意图，系统则强制回退至EAGLE3等神经草稿模型。

*   **推理优化：**
    引入块三角注意力（Block-Triangular Attention）机制，允许目标模型在一次前向传递中并行验证多个候选草稿，并利用KV Cache延迟同步技术，显著降低了多模式切换带来的缓存对齐开销。

#### 5. 实验实现与对比
*   **实验设置：** 在 NVIDIA H100 GPU 上进行；评估模型涵盖 Llama-3.1-8B/70B 和 Qwen3-8B 系列。
*   **基线对比：** 对比了启发式方法（PLD、CopySpec）、神经草稿（EAGLE3）以及统计自适应方法（BanditSpec）。
*   **主要结果：**
    *   在HumanEval数据集上，SwitchSD实现了高达 2.58× 的推理加速，较EAGLE3进一步提升了约15%的吞吐量。
    *   通过对比发现，SwitchSD在更少的复制尝试次数下，获得了更长的平均接受长度（Mean Acceptance Length），验证了探测器对“高价值复制区间”的精准捕获能力。
    *   即便在T=1.0的高温采样环境下，SwitchSD依然保持了优于EAGLE3的性能，证明了该方法的鲁棒性。

#### 6. 关键技术细节与局限性
*   **架构优势：** SwitchSD对草稿模型不可知（Draft-agnostic），无论是使用EAGLE3还是基础的Speculative Sampling（SPS），SwitchSD均能通过精确的切换机制实现增益。
*   **数据构建：** 作者构建了 *CopyDiversity* 数据集，包含从强复制任务（代码块）到弱复制任务（开放性写作）的多种场景，确保了探测器对各种上下文结构的泛化能力。
*   **潜在局限：** 
    *   该方法需要对不同模型预先训练和微调探测器权重，虽然是轻量级的，但增加了部署前的配置复杂度。
    *   对于长尾的、非典型上下文，探测器的表现可能会随模型层数的微小变化而波动，仍需进行细致的超参数阈值调整。
    *   目前方法主要针对单机推理加速，在多机模型并行场景下的通信开销未详细讨论。

**总结：** SwitchSD 将Speculative Decoding的控制权从单纯的“概率统计”提升到了“模型内在表征感知”的层面，通过“白盒”信号过滤掉了偶然噪声，是当前大模型高效推理领域的一次重要范式转变。
